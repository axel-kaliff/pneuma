#!/bin/bash
# pneuma override — there is no AUR on Fedora/bootc; same story as
# omarchy-pkg-add.

# omarchy:summary=No AUR on pneuma — use brew or flatpak
# omarchy:args=<packages...>

exec omarchy-pkg-add "$@"
