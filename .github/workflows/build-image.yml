---
name: Build and Push Image

# Two-branch model:
#   main   = testing branch, publishes :stable-testing (plus :testing for the
#            factory release gate)
#   stable = production branch, publishes :stable
# promote-main-to-stable.yml opens the main -> stable promotion PR.
#
# PR builds are disabled by default to save CI minutes. To enable them, add:
#   pull_request:
#     branches:
#       - main
#     paths-ignore:
#       - "**.md"
#       - ".github/workflows/*validate*.yml"
on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**.md"
      - ".github/workflows/*validate*.yml"
  push:
    branches:
      - main
      - stable
    paths-ignore:
      - "**.md"
      - ".github/workflows/*validate*.yml"
  schedule:
    # Weekly rebuild (Mon 05:00 UTC): picks up CS10/EPEL/COPR package updates
    # even when no commit lands. Cheap now that the omarchy stack comes
    # prebuilt from COPR instead of being compiled in-image.
    - cron: "0 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

env:
  IMAGE_NAME: "${{ github.event.repository.name }}"
  IMAGE_VENDOR: "${{ github.repository_owner }}"
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  IMAGE_DESC: "Pneuma - Omarchy (Hyprland + Quickshell) and GNOME on Bluefin LTS (CentOS Stream 10 bootc), with Ghostty"
  IMAGE_KEYWORDS: "bootc,ublue,universal-blue"
  IMAGE_LOGO_URL: "https://avatars.githubusercontent.com/u/120078124?s=200&v=4"
  DEFAULT_TAG: "stable"
  PRODUCTION_BRANCH: "stable" # The branch that publishes :stable production images
  REGISTRY_CACHE_WRITE: ${{ github.event_name != 'pull_request' && '1' || '0' }}
  # Rechunking is REQUIRED here, not an optimization: GHCR enforces a 10-minute
  # timeout per blob upload, and this image's monolithic build layer blows past
  # it (first push died at 9m58s with an HTML 400 from GitHub's front end).
  # Chunkah splits the image into <=128 small pre-compressed layers so no
  # single upload approaches the limit — and OTA deltas shrink as a bonus.
  ENABLE_RECHUNKING: "true"
  RECHUNK_MAX_LAYERS: "128"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  # Stage-1 gate: full depsolve + download + transaction test of the Omarchy
  # runtime set inside the real base image (~3 min). Catches COPR repo
  # breakage, versionlock conflicts, and broken RPMs long before the 30-min
  # image build does — and on schedule runs, even without any commit.
  depsolve-check:
    name: Fast COPR depsolve check
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Test-install the Omarchy runtime set against the base image
        shell: bash
        run: |
          podman run --rm ghcr.io/ublue-os/bluefin-dx:lts-hwe bash -c '
            set -euo pipefail
            for repo in akaliff/pneuma yselkowitz/wlroots-epel; do
              id="copr-${repo//\//-}"
              printf "%s\n" \
                "[${id}]" \
                "name=${id}" \
                "baseurl=https://download.copr.fedorainfracloud.org/results/${repo}/epel-10-\$basearch/" \
                "gpgcheck=1" \
                "gpgkey=https://download.copr.fedorainfracloud.org/results/${repo}/pubkey.gpg" \
                "enabled=1" > "/etc/yum.repos.d/${id}.repo"
            done
            dnf -y install --setopt=tsflags=test --enablerepo=epel-multimedia \
              hyprland hyprland-guiutils hyprpicker hyprsunset \
              xdg-desktop-portal-hyprland starship omedora libinput \
              libxkbcommon foot grim slurp wtype brightnessctl keyd
            echo "depsolve OK"'

  build:
    name: Build and push image
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Prepare environment
        run: |
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> "${GITHUB_ENV}"
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> "${GITHUB_ENV}"

      # Anything not on the production branch is a testing build. TAG_STREAM
      # keeps generate-tags on the "testing" stream so it never emits
      # stable-daily* aliases that would collide with production; the
      # Finalize branch tags step renames them to stable-testing-*.
      - name: Determine image tag
        id: determine-tag
        shell: bash
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          if [[ "${REF_NAME}" != "${PRODUCTION_BRANCH}" ]]; then
            echo "DEFAULT_TAG=${DEFAULT_TAG}-testing" >> "${GITHUB_ENV}"
            echo "TAG_STREAM=testing" >> "${GITHUB_ENV}"
            echo "Building testing image: ${DEFAULT_TAG}-testing"
          else
            echo "TAG_STREAM=${DEFAULT_TAG}" >> "${GITHUB_ENV}"
            echo "Building production image: ${DEFAULT_TAG}"
          fi

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Preflight
        id: preflight
        uses: projectbluefin/actions/bootc-build/preflight@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          registry: ghcr.io
          github-token: ${{ secrets.GITHUB_TOKEN }}
          image-name: ${{ env.IMAGE_NAME }}

      - name: Setup runner
        uses: projectbluefin/actions/bootc-build/setup-runner@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          storage-backend: btrfs
          install-tools: '["just"]'

      # The 2026-08-10 runner image switched to a static podman bundle whose
      # /etc/containers/storage.conf forces mount_program = fuse-overlayfs
      # even for rootful podman. fuse-overlayfs corrupts small files
      # rewritten in a layer at commit time (zeroed tails -> dnf.conf
      # "Missing '=' on line 4", cf. containers/fuse-overlayfs#333) and
      # forces naive whole-filesystem diffs through FUSE, turning trivial
      # layer commits into 10-16 minute stalls. Cleaning storage.conf alone
      # is NOT enough: any earlier podman run stamps
      # overlay/.has-mount-program=true into the graph root, after which
      # containers/storage silently re-execs fuse-overlayfs forever while
      # `podman info` still reports a clean config. So: clean the config,
      # wipe the storage state, delete the binary so no auto-detection can
      # resurrect it, then assert the store is really native. No-op safe if
      # setup-runner's btrfs loopback ever works again (no /mnt disk on
      # current runners, so it silently skips).
      - name: Force native kernel overlayfs storage
        shell: bash
        run: |
          sudo sed -i -e '/^mount_program/d' -e 's/^mountopt.*/mountopt = "nodev"/' \
            /etc/containers/storage.conf
          sudo podman system reset --force
          sudo rm -f /usr/local/bin/fuse-overlayfs /usr/bin/fuse-overlayfs
          sudo podman info --format 'storage: {{.Store.GraphDriverName}} {{.Store.GraphOptions}}'
          flag=$(sudo cat /var/lib/containers/storage/overlay/.has-mount-program 2>/dev/null || echo absent)
          echo "mount-program flag: ${flag}"
          if [[ "${flag}" == "true" ]] || sudo grep -q '^mount_program' /etc/containers/storage.conf; then
            echo "storage would still use fuse-overlayfs; refusing to build (see PR #9)"
            exit 1
          fi

      - name: Restore DNF cache
        id: dnf-cache-restore
        uses: projectbluefin/actions/bootc-build/dnf-cache@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          action: restore
          cache-name: ${{ env.IMAGE_NAME }}
          cache-bust: ${{ hashFiles('**/Containerfile') }}

      - name: Detect changes
        id: detect-changes
        uses: projectbluefin/actions/bootc-build/detect-changes@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          filters: |
            image:
              - 'Containerfile'
              - 'build/**'
              - 'custom/**'
              - 'Justfile'

      - name: Build image
        if: steps.detect-changes.outputs.should_build == 'true' || github.event_name != 'pull_request'
        id: build-image
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          IMAGE_VENDOR: ${{ env.IMAGE_VENDOR }}
          DEFAULT_TAG: ${{ env.DEFAULT_TAG }}
          REGISTRY_CACHE_WRITE: ${{ env.REGISTRY_CACHE_WRITE }}
        with:
          timeout_minutes: 120
          max_attempts: 3
          retry_wait_seconds: 300
          command: |
            sudo -E "$(command -v just)" build "${IMAGE_NAME}" "${DEFAULT_TAG}"

      - name: Get version label
        if: steps.detect-changes.outputs.should_build == 'true' || github.event_name != 'pull_request'
        id: version
        shell: bash
        env:
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          DEFAULT_TAG: ${{ env.DEFAULT_TAG }}
        run: |
          VERSION_LABEL=$(sudo podman inspect "localhost/${IMAGE_NAME}:${DEFAULT_TAG}" \
            --format '{{index .Config.Labels "org.opencontainers.image.version"}}')
          echo "version=${VERSION_LABEL}" >> "${GITHUB_OUTPUT}"
          echo "Version: ${VERSION_LABEL}"

      - name: Generate tags
        if: steps.detect-changes.outputs.should_build == 'true' || github.event_name != 'pull_request'
        id: generate-tags
        uses: projectbluefin/actions/bootc-build/generate-tags@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          base-name: ${{ env.IMAGE_NAME }}
          stream-name: ${{ env.TAG_STREAM }}
          version-label: ${{ steps.version.outputs.version }}
          event-name: ${{ github.event_name }}
          pr-number: ${{ github.event.number }}

      # generate-tags emits stream-native tags (testing* or stable-daily* plus
      # version aliases). For testing builds this renames testing* tags to
      # stable-testing-*, keeps the bare :testing tag (the factory release
      # gate in reusable-promote-squash.yml resolves digests from it), and
      # guarantees the mutable consumer tag (:stable / :stable-testing) is
      # always published.
      - name: Finalize branch tags
        if: steps.detect-changes.outputs.should_build == 'true' || github.event_name != 'pull_request'
        id: branch-tags
        shell: bash
        env:
          GENERATED_TAGS: ${{ steps.generate-tags.outputs.tags }}
        run: |
          read -r -a generated_tags <<< "${GENERATED_TAGS}"
          branch_tags=()

          add_tag() {
            local candidate="$1"
            local existing
            for existing in "${branch_tags[@]}"; do
              [[ "${existing}" == "${candidate}" ]] && return
            done
            branch_tags+=("${candidate}")
          }

          for tag in "${generated_tags[@]}"; do
            if [[ "${TAG_STREAM}" == "testing" ]]; then
              # Bare :testing stays so the promotion release gate can resolve it.
              [[ "${tag}" == "testing" ]] && add_tag "${tag}"
              add_tag "${tag/testing/stable-testing}"
            else
              add_tag "${tag}"
            fi
          done
          add_tag "${DEFAULT_TAG}"

          echo "tags=${branch_tags[*]}" >> "${GITHUB_OUTPUT}"
          echo "default-tag=${DEFAULT_TAG}" >> "${GITHUB_OUTPUT}"

      - name: Save DNF cache
        if: always() && !cancelled() && github.event_name != 'pull_request'
        uses: projectbluefin/actions/bootc-build/dnf-cache@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          action: save
          cache-name: ${{ env.IMAGE_NAME }}
          allow-write: ${{ github.event_name != 'pull_request' && 'true' || 'false' }}
          cache-bust: ${{ hashFiles('**/Containerfile') }}

      # OPTIONAL: Set ENABLE_RECHUNKING to "true" above to optimize OTA deltas.
      # This uses OCI-native chunkah and does not require bootc-base-imagectl.
      - name: Rechunk image
        if: env.ENABLE_RECHUNKING == 'true' && github.event_name != 'pull_request'
        id: rechunk-image
        uses: projectbluefin/actions/bootc-build/chunka@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          source-image: localhost/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}
          max-layers: ${{ env.RECHUNK_MAX_LAYERS }}

      - name: Tag images
        if: github.event_name != 'pull_request'
        shell: bash
        env:
          ALIAS_TAGS: ${{ steps.branch-tags.outputs.tags }}
        run: |
          sudo -E "$(command -v just)" tag-images "${IMAGE_NAME}" \
                          "${DEFAULT_TAG}" \
                          "${ALIAS_TAGS}"

      # Publish from both the testing (main) and production (stable) branches;
      # PR builds only build and validate, they never publish.
      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to GHCR
        if: github.event_name != 'pull_request'
        id: push
        uses: projectbluefin/actions/bootc-build/push-image@8906e13e3ac1654504e75ad27440bffb28e52fbc # v1
        with:
          image-name: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.branch-tags.outputs.tags }}
          default-tag: ${{ steps.branch-tags.outputs.default-tag }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      # Key-based cosign signing (image-template pattern). Key-based rather
      # than keyless because the host-side containers-policy.json verification
      # (sigstoreSigned + keyPath, the ublue precedent) understands it
      # directly; cosign.pub is committed at the repo root and later shipped
      # in-image for `bootc switch --enforce-container-sigpolicy`.
      # --new-bundle-format=false: the old signature format is what
      # ostree/bootc host verification reads.
      - name: Install cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
      - name: Sign container image
        if: github.event_name != 'pull_request'
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
          DIGEST: ${{ steps.push.outputs.digest }}
        run: |
          cosign sign -y --new-bundle-format=false --use-signing-config=false \
            --key env://COSIGN_PRIVATE_KEY "${IMAGE_REGISTRY}/${IMAGE_NAME}@${DIGEST}"

      - name: Build telemetry summary
        if: steps.detect-changes.outputs.should_build == 'true' || github.event_name != 'pull_request'
        shell: bash
        run: |
          IMAGE_SIZE=$(sudo podman image inspect "localhost/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}" \
            --format '{{.Size}}' 2>/dev/null || echo "0")
          IMAGE_SIZE_MB=$(( IMAGE_SIZE / 1024 / 1024 ))
          LAYER_COUNT=$(sudo podman image inspect "localhost/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}" \
            --format '{{len .RootFS.Layers}}' 2>/dev/null || echo "?")

          {
            echo "## Build Telemetry: ${{ env.IMAGE_NAME }}"
            echo ""
            echo "**Image size (uncompressed):** ${IMAGE_SIZE_MB} MB"
            echo "**Layer count:** ${LAYER_COUNT}"
          } >> "$GITHUB_STEP_SUMMARY"
